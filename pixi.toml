[workspace]
name = "claude-science-container"
version = "0.1.0"
channels = ["conda-forge", "bioconda"]
platforms = ["linux-64", "linux-aarch64", "osx-arm64"]

[tasks]
build = { cmd = "CGO_ENABLED=0 go build -o launcher .", cwd = "helper" }

[dependencies]
go = ">=1.26.4,<2"
