{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "references": {
    "docker": {
      "path": ".",
      "description": "Use for Dockerfile, docker-compose.yml, and container scripts"
    }
  },
  "command": {
    "build": { "description": "Build the Docker image", "prompt": "Run `docker compose build` to build the Claude-Science-Container image" },
    "up": { "description": "Start the container", "prompt": "Run `docker compose up -d` to start the container in detached mode" },
    "down": { "description": "Stop the container", "prompt": "Run `docker compose down` to stop and remove the container" },
    "logs": { "description": "View container logs", "prompt": "Run `docker compose logs -f` to follow container logs" }
  }
}
